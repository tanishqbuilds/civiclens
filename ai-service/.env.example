# AI Service Environment Variables
PORT=5000
FLASK_ENV=development

# MongoDB URI — Required for Neuro-Fuzzy Reputation Scoring
# Must match the same MongoDB instance used by the Node.js server
MONGO_URI=mongodb://<user>:<password>@<host>/<database>?ssl=true&replicaSet=...&authSource=admin
